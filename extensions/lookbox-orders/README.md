# RealCloset 구매내역 확장

로그인된 **내 크롬**에서 쇼핑몰 주문내역을 읽어 RealCloset 옷장 후보로 넘긴다.
아이디·비밀번호는 저장하지 않는다.

## 한 번만 연결

1. 크롬 주소창에 `chrome://extensions` 를 연다.
2. 오른쪽 위 **개발자 모드**를 켠다.
3. **압축해제된 확장 프로그램 로드** → 이 폴더(`extensions/lookbox-orders`)를 고른다.

RealCloset을 새로고침한 뒤 아이템 추가 → 구매내역 → 쇼핑몰 선택 → **주문 내역 가져오기**.
